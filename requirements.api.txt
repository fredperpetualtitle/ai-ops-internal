# ---------------------------------------------------------------------------
# requirements.txt — Railway / Cloud API deployment
# ---------------------------------------------------------------------------
# This file contains ONLY the dependencies needed to run the FastAPI API
# server on Linux (Railway).  Windows-only and heavy data-science packages
# are split out into requirements.local.txt / requirements.scraper-windows.txt.
# ---------------------------------------------------------------------------

# --- Core API framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0

# --- LLM / embeddings ---
openai>=1.0.0

# --- Vector store ---
chromadb>=0.4.0

# --- Google Sheets (KPI read) ---
gspread>=6.0.0
google-auth

# --- General ---
python-dotenv>=1.0.0
requests>=2.28.0
httpx>=0.24.0
