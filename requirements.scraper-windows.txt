# ---------------------------------------------------------------------------
# requirements.scraper-windows.txt — Windows-only Outlook COM scraper
# ---------------------------------------------------------------------------
# Install these ONLY on your local Windows dev machine.
# These packages will NOT work on Linux / Railway.
# ---------------------------------------------------------------------------

pywin32
python-dotenv
pyyaml
requests
beautifulsoup4
google-api-python-client
google-auth
google-auth-oauthlib
pandas
openpyxl
pypdf
pdfminer.six
xlrd
python-docx

# LLM extraction layer (GPT-4o)
openai>=1.0.0

# OCR / scanned-PDF support (local, no API calls)
pdf2image
pytesseract
pillow
rapidfuzz
opencv-python

# Email RAG + API server (System 2 & 3)
chromadb>=0.4.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gspread>=6.0.0
